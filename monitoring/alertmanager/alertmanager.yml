global:
  resolve_timeout: 5m

route:
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: "discord"

receivers:
  - name: "discord"
    webhook_configs:
      - url: "http://localhost:9093/api/v2/alerts"
        send_resolved: true
